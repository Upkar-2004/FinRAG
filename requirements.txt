# --- Core RAG pipeline (Day 1) ---
pdfplumber>=0.11,<0.12              # PDF text and table extraction
langchain-text-splitters>=1.1,<2.0  # RecursiveCharacterTextSplitter
sentence-transformers>=5.6,<6.0     # local BGE embeddings
chromadb>=1.5,<2.0                  # persisted local vector store

# --- LLM clients (Day 1) ---
groq>=1.5,<2.0             # Groq client with reasoning/tool-call parameters

# --- Hybrid retrieval + eval (Day 2) ---
rank-bm25>=0.2,<0.3        # keyword (BM25) retrieval baseline
pandas>=2.0,<4.0           # tabular evaluation analysis
numpy>=1.26,<3.0

# --- Config / UI (Day 1 / Day 3) ---
python-dotenv>=1.0,<2.0    # load GROQ_API_KEY from a local .env file
streamlit>=1.59,<2.0       # demo UI; st.html JS flag and current widget APIs

# --- Transitive security floors (audited 2026-08-03) ---
# Chroma -> kubernetes -> aiohttp; pdfplumber -> pdfminer -> cryptography;
# Streamlit -> GitPython. Keep these explicit so a fresh install cannot resolve
# to versions with known, already-patched advisories.
aiohttp>=3.14.3,<4.0
cryptography>=50.0,<51.0
GitPython>=3.1.57,<4.0

# Note on reproducibility: these are minimum-version pins so a clean clone
# installs cleanly across platforms. Once your project works, run
#   pip freeze > requirements.lock.txt
# to capture exact versions for a fully frozen, reproducible environment.
